<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	<select id="boardList" resultType="com.example.spring05.modelVO.boardVO">
		select seq, title, writer, content, regdate, modate, cnt, passwd from spboard order by seq desc
	</select>
	
	<insert id="boardWrite">
		insert into spboard (seq, title, writer, content, passwd) values (spboard_seq.nextval, #{title}, #{writer}, #{content}, #{passwd})
	</insert>
	
	<select id="boardView" resultType="com.example.spring05.modelVO.boardVO">
		select seq, title, writer, content, regdate, modate, cnt, passwd, from spboard where seq = #{seq}
	</select>
	
	<update id="boardModify">
		update spboard set title = #{title}, writer = #{writer}, content = #{content}, passwd = #{passwd} where seq = #{seq}
	</update>
	
	<delete id="boardDel">
		delete from spboard where seq = #{seq}
	</delete>

</mapper>